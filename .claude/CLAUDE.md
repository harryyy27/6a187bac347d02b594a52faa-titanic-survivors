## Database & Infrastructure Provisioning

If any step requires a database (MongoDB or PostgreSQL):
1. Call `check_database_exists` first with the relevant .env file path and variable name.
2. Only call `create_mongo_database` or `create_postgres_database` if it does not already exist.
   These tools provision a real hosted database and write the connection string to the env file.
3. Never spin up a local database process or use docker-compose for database services — use the tools above.

## Preview Compose Setup (MANDATORY)

During setup, create or update the project-root `docker-compose.dev.yml` (or project-root `docker-compose.yml` if that is the project's canonical compose file) with one app service per deployable/hostable container. Omit externally managed infrastructure services unless the project intentionally owns them. Component-local compose files may exist for documentation, but they are not the preview source of truth. Never leave a root compose file with `services: {}` when the project has hostable components.

For each deployable service, define:
- service key/name
- `build.context` and `build.dockerfile` pointing at the service runtime Dockerfile
- container port(s) via `ports` or `expose`
- startup `command`
- optional `x-install-command` when dependencies baked into `/deps` must be linked or exposed in the live workspace

Use stable service keys that match the component role, such as `web`, `frontend`, `client`, `ui`, `api`, `backend`, or the component name. The browser-facing service should be named with a frontend/web alias when applicable because preview routing selects the entry service from those names and ports.

Check every compose service against its corresponding runtime Dockerfile (`Dockerfile`, `Dockerfile.dev`, `Dockerfile.prod`, or the Dockerfile referenced by the project). The compose ports, command/entrypoint intent, working directory assumptions, and dependency discovery must match the Dockerfile. If they differ, fix the configuration rather than guessing.

# Project: Titanic Survivors



---

# Component: api

**Language:** python 3.11
**Packages:** fastapi, uvicorn, pydantic, pandas, numpy, scikit-learn, xgboost, joblib, python-dotenv, orjson, starlette, aiofiles, requests, typing-extensions

---

# Your Task

Scaffold the **api** component from scratch. Work through each phase in order. All work happens inside the `api` folder at the project root. Do NOT install packages directly in the worker workspace; declare runtime, dev, lint, and test dependencies in the component manifest file. Do NOT run the application or tests.

## Phase 1: Environment Setup

1. 1) From the project root, create a .gitignore file inside api that excludes build artifacts and local runtime state while retaining tests by listing: .venv/, .env, .env.*, __pycache__/, *.pyc, *.pyo, *.pyd, .mypy_cache/, .pytest_cache/, .ruff_cache/, .DS_Store, .idea/, .vscode/, dist/, build/, .coverage, htmlcov/, .ipynb_checkpoints/, .cache/, .cl/, .claude/, .python-version, node_modules/, data/, notebooks/, .docker-sync/, .envrc, .env.local, .env.development.local, .env.test.local, .env.production.local
2. 2) From the project root, define dependency isolation for the api component by adopting Poetry as the sole package/dependency manager; plan to maintain a pyproject.toml manifest and a poetry.lock lockfile exclusively within api
3. 3) From the project root, create a pyproject.toml inside api configured for Poetry with project metadata (name: titanic-survivors-api, version: 0.1.0, description: FastAPI service for Titanic Survivors, authors: placeholder), set the required Python to 3.11 series, and add all runtime dependencies with no version specifiers so the latest will be resolved at lock time: fastapi, uvicorn, pydantic, pandas, numpy, scikit-learn, xgboost, joblib, python-dotenv, orjson, starlette, aiofiles, requests, typing-extensions
4. 4) From the project root, in the same pyproject.toml inside api, add development-only dependencies (no version specifiers) to support linting and type checking: ruff and mypy
5. 5) From the project root, in pyproject.toml inside api, add tool configuration for Ruff to enforce linting rules: enable line-length 100, target-version py311, select rules E,F,I,B,UP,S,ANN,COM,Q,RET, and exclude typical build and cache directories; also configure Ruff to fix issues automatically on request and to ignore no tests directory
6. 6) From the project root, in pyproject.toml inside api, add tool configuration for MyPy suitable for a FastAPI project: python_version 3.11, strict optional enabled, warn_unused_ignores true, disallow_untyped_defs true, ignore_missing_imports true for third-party libs, and set mypy cache directory to .mypy_cache
7. 7) From the project root, document the standard lint command for this component inside pyproject.toml’s [tool.ruff] as the intended development command string: poetry run ruff check --fix .; this will be the command to pass to a subprocess in CI or locally
8. 8) From the project root, create a .env file inside api with temporary dummy values: ENV=development, APP_NAME=Titanic Survivors API, LOG_LEVEL=info, SECRET_KEY=dev-secret-change-me, MODEL_PATH=./models/titanic_clf.joblib, ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,*.appbuilder.127.0.0.1.nip.io, PORT=8000
9. 9) From the project root, initialize the application package structure inside api by creating the directories: api/app, api/app/api, api/app/core, api/app/ml, api/app/static, and ensure each Python package directory includes an empty __init__.py
10. 10) From the project root, add a configuration module inside api at app/core/config.py that loads .env using python-dotenv, reads environment variables via os.environ, parses the ALLOWED_HOSTS list, exposes a Settings object with attributes (app_name, env, log_level, secret_key, model_path, allowed_hosts, port), and provides a get_settings() function returning a cached singleton
11. 11) From the project root, add the FastAPI application entrypoint inside api at app/main.py that: creates FastAPI with ORJSONResponse as default, attaches Starlette’s TrustedHostMiddleware using allowed hosts from settings including localhost, 127.0.0.1, 0.0.0.0, and wildcard subdomains like *.appbuilder.127.0.0.1.nip.io, mounts a /static route backed by the static directory using aiofiles, includes the API router defined below under prefix /api, and exposes an application factory for ASGI servers
12. 12) From the project root, add the primary API router inside api at app/api/routes.py that defines health and readiness endpoints (/healthz and /readyz) returning simple JSON payloads, and an inference endpoint (/predict) that accepts a JSON body with typical Titanic features (e.g., Pclass, Sex, Age, SibSp, Parch, Fare, Embarked), validates with Pydantic models, converts to a pandas DataFrame, calls the model predictor (defined below), and returns survival probability and label
13. 13) From the project root, add Pydantic models inside api at app/api/schemas.py to define request and response shapes for the /predict endpoint, using type annotations compatible with pydantic v2 and providing field defaults where appropriate
14. 14) From the project root, add the model integration inside api at app/ml/model.py that lazily loads a classifier from MODEL_PATH via joblib when first used, includes a thread-safe singleton loader, and provides a predict_proba_and_label(dataframe) function; if the model file is missing, raise a clear runtime error indicating the path and expected format
15. 15) From the project root, add an ASGI module inside api at app/__init__.py that imports and exposes app from app/main.py so the ASGI entrypoint is discoverable as app:app by servers
16. 16) From the project root, add a README.md inside api that documents: component purpose, how dependencies are managed via Poetry, how to generate a lockfile with the installer Dockerfile, how to run linting using the recorded command, and which environment variables are required
17. 17) From the project root, create a .dockerignore file inside api that excludes files not needed in the image build context, including: .git, .gitignore, .venv, __pycache__, *.pyc, *.pyo, .mypy_cache, .pytest_cache, .ruff_cache, .DS_Store, .idea, .vscode, dist, build, data, notebooks, .coverage, htmlcov, .env, .env.*, .claude, README.md, and the entire tests directory
18. 18) From the project root, create a Dockerfile inside api that: uses python:3.11-slim as the sole base image, sets WORKDIR to /app, installs Poetry non-interactively, copies only pyproject.toml and poetry.lock first, configures Poetry to not create virtualenvs (so dependencies install into the container’s system site-packages), runs poetry install --no-root --no-interaction to install dependencies, then copies the remaining api application source files, sets environment variables PYTHONDONTWRITEBYTECODE=1 and PYTHONUNBUFFERED=1, and declares the default container command to be uvicorn app:app with host 0.0.0.0 and port read from PORT at runtime (without actually running it during this setup)
19. 19) From the project root, create a Dockerfile.installer file inside api (service: installer) that uses the exact same python:3.11-slim base image, sets WORKDIR to /app, installs Poetry non-interactively, copies only pyproject.toml from api into /app, and runs a non-interactive lock resolution step that generates poetry.lock from the manifest without installing packages; this file exists solely to produce a reproducible lockfile artifact
20. 20) From the project root, ensure the pyproject.toml inside api is finalized to contain: [tool.poetry] metadata; [tool.poetry.dependencies] with python version 3.11 range and the listed runtime packages each set to "*"; [tool.poetry.group.dev.dependencies] with ruff and mypy set to "*"; [tool.ruff] configuration per step 5; [tool.mypy] configuration per step 6; and an explicit note in comments that the intended lint command is: poetry run ruff check --fix .
21. 21) From the project root, create seed files and directories inside api for operational readiness: an empty models/ directory to hold the trained model artifact referenced by MODEL_PATH, and ensure static/ exists for any static assets served by the API
22. 22) From the project root, verify that no files under api/tests are ignored by .gitignore and that no testing tools or settings are modified, preserving the pre-existing tests/unit and tests/integration structure exactly as-is
23. Step 22: Deduce the app’s entry point path from the architecture_context and pass it to inject_script_into_entry_point to inject the script.
24. Step 23: Create a Dockerfile.dev for preview hot reload. It must install dependencies into a location outside the source mount such as /deps or a virtual environment, set the runtime working directory to /workspace/repo/api, use the mounted source directory rather than copied source, expose the service port, and run the development server with file-watching, auto-reload, or polling enabled. Also create or update the project-root docker-compose.dev.yml: add a service entry for `api` with build.context, build.dockerfile, ports/expose, command, and, if applicable, x-install-command. Do not create only a component-local compose file, and do not leave the root compose as `services: {}` when this hostable component exists. Verify the compose service key, port(s), command, working directory assumptions, and x-install-command match the Dockerfile.dev you created for `api`.

## Phase 2: Test Environment Setup

1. Inside api, create a requirements-test.txt dedicated to testing and list only the test-time dependencies needed to run and configure the test suite. Include: pytest. This keeps the test image lean, ensures dependency isolation for tests, and avoids pulling in production-only packages.
2. Inside api, add a pytest.ini to fully configure pytest for non-interactive CI execution. Set test discovery to the tests directory, disable warnings as errors, set quiet output, and ensure colorized, deterministic runs. Also fix the test path root so relative paths in failure messages are stable.
3. Inside api, add a .env.test file containing safe defaults for test runs (for example, ENV=testing, LOG_LEVEL=WARNING, and PORT=8001). This allows the test process to consistently read a predictable environment without depending on developer machines.
4. Inside api, create a minimal project-owned module tiny_math.py that exposes a deterministic function add(a, b) returning a + b. This serves as the smallest possible internal code artifact for the smoke test and confirms the test runner executes our code path.
5. Inside api/tests, create a file testWrapperTimeout implementing a reusable wrapper/decorator named testWrapperTimeout that accepts a test function and an optional timeout parameter defaulting to 10 seconds. The wrapper must execute the test function in a background thread and, if the timeout elapses, raise a TimeoutError that clearly includes the test name, file path, and starting line number. The implementation should: (1) accept either direct decorator usage (@testWrapperTimeout) or parameterized usage (@testWrapperTimeout(timeout=5)); (2) extract metadata from the wrapped function via func.__code__.co_filename, func.__code__.co_firstlineno, and func.__name__; (3) use threading.Thread to run the test function, join with the specified timeout, and, if still alive, raise TimeoutError with a message containing the test name, absolute file path, and line; and (4) preserve the wrapped function’s __name__ and __doc__ via functools.wraps.
6. Inside api/tests, create a conftest.py that prepares the test environment and guarantees cleanup after each test. Implement: (a) an autouse fixture that snapshots os.environ at setup and restores it at teardown to prevent side effects; (b) lightweight stubs inserted into sys.modules for heavy, unused production packages (e.g., fastapi, uvicorn, pydantic, pandas, numpy, sklearn, xgboost, joblib, python_dotenv, orjson, starlette, aiofiles, requests, typing_extensions) so import resolution in any incidental discovery remains fast without installing those packages in the test image; and (c) no I/O outside the working directory. This keeps tests hermetic and fast.
7. Inside api/tests/unit, add a smoke test file test_smoke.py that imports the project-owned tiny_math.add function and decorates each test with @testWrapperTimeout to enforce the timeout. Write a single test function test_add_returns_sum that asserts add(2, 3) == 5. The test must not import or call any external libraries and must clean up any temporary state (none is created in this test, so the autouse fixture suffices).
8. Inside api, create Dockerfile.test using the official Python 3.11 slim image to match the component’s runtime. Set WORKDIR to /app. Copy only the explicitly required files: requirements-test.txt, pytest.ini, .env.test, tiny_math.py, the entire tests directory (including unit and integration subfolders), and any auxiliary configuration files used by tests. Install only the test dependencies from requirements-test.txt. Do not copy or install production dependencies because our tests only validate the harness and project-owned code paths, not third-party packages.
9. Inside api, create Dockerfile.test.dockerignore to keep the test image lean and avoid leaking binary/data artifacts into the build context. Explicitly exclude: *.pkl, *.pickle, *.h5, *.pt, *.pth, *.bin, *.npy, *.npz, *.joblib, *.ckpt, *.model, *.safetensors, *.onnx, data/, models/, outputs/, results/, checkpoints/. Do not exclude the tests/ directory so the tests remain available to the image.
10. Inside api, document in a README-test.md (test-focused notes) how the wrapper must be applied to every new test function by using the @testWrapperTimeout decorator, how environment variables are isolated via .env.test and the autouse fixture, and that dependencies for tests are restricted to what is declared in requirements-test.txt to keep the test image minimal.
